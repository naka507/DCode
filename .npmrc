# npm project config.
# electron-builder needs a flat-enough tree for its native rebuild step, and the
# legacy peer ranges in the folded agent-runtime deps are not npm-clean.
legacy-peer-deps=true
fund=false
audit=false
